# Post–Terraform next steps — what’s left

Staging Vault is done (all 5 secrets in ai-factory-staging). Below is what you still need to do.

---

## 1. Vercel – set real anon keys

Terraform set placeholders. Replace them with real keys:

| Variable | Environment | Where to get the value |
|----------|-------------|-------------------------|
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | **Preview** | [Staging → Settings → API](https://supabase.com/dashboard/project/anqhihkvovuhfzsyqtxu/settings/api) → “anon” “public” key |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | **Production** | [Prod → Settings → API](https://supabase.com/dashboard/project/wxupyzthtzeckgbzqmjy/settings/api) → “anon” “public” key |

In **Vercel → ai-factory-console → Settings → Environment Variables**, edit each and paste the correct key (or add both if they’re missing).

---

## 2. Vercel – production branch

**Vercel → ai-factory-console → Settings → Git** → set **Production branch** to `prod`.

So: `main` = preview (staging), `prod` = production.

---

## 3. Prod Supabase – DB password and DATABASE_URL

The prod DB password was generated by Terraform and is not in `terraform output` (sensitive).

- **If you didn’t save it:**  
  **Supabase → ai-factory-prod → Project Settings → Database** → **Reset database password**, set a new one, then use that in the connection string below.

- **If you want Terraform to use a known password next time:**  
  Re-apply with  
  `supabase_db_password_prod=YourChosenPassword`  
  (and other required vars). You can then use that same password for `DATABASE_URL`.

Build the **prod** connection string:

- **Supabase → ai-factory-prod → Project Settings → Database** → **Connection string** → **URI** (Session or Transaction pooler).
- Replace `[YOUR-PASSWORD]` with the prod DB password from above.

You’ll use this in step 5 for Render prod.

---

## 4. Prod Supabase – migrations

Apply the same schema to prod:

```bash
supabase link --project-ref wxupyzthtzeckgbzqmjy
supabase db push
```

(Or run your migration SQL in **Prod → SQL Editor** if you don’t use the CLI.)

---

## 5. Prod Supabase – Vault

Staging Vault is already filled. Do the same for **prod**:

- **Supabase → ai-factory-prod → Integrations → Vault → Secrets** → **Add new secret** for each:
  - `github_token`, `openai_api_key`, `vercel_api_token`, `supabase_access_token`, `render_api_key`

Use the same values as staging (or prod-specific ones if you prefer). Direct link: [Prod Vault](https://supabase.com/dashboard/project/wxupyzthtzeckgbzqmjy/settings/vault).

---

## 6. Render – prod Control Plane DATABASE_URL

**Render → ai-factory-api-prod → Environment** → set **DATABASE_URL** to the **prod** Supabase connection string from step 3 (same password, prod project host).

Redeploy the prod service after saving so it picks up the new env var.

---

## Quick reference

| Item | Staging | Prod |
|------|--------|------|
| Supabase project ref | `anqhihkvovuhfzsyqtxu` | `wxupyzthtzeckgbzqmjy` |
| Supabase URL | `https://anqhihkvovuhfzsyqtxu.supabase.co` | `https://wxupyzthtzeckgbzqmjy.supabase.co` |
| Anon key | Vercel **Preview** | Vercel **Production** |
| Control Plane | ai-factory-api-staging (main) | ai-factory-api-prod (prod) |
| DATABASE_URL | Set on staging service (Render) | Set on prod service (Render) |
| Vault | Done | Add 5 secrets (step 5) |

---

## Verification (did I finish everything?)

Use this to confirm each step:

| # | Step | How to verify |
|---|------|----------------|
| 1 | Vercel anon keys | Vercel → ai-factory-console → Environment Variables. For **Preview** and **Production**, `NEXT_PUBLIC_SUPABASE_ANON_KEY` should show a value (not `REPLACE_AFTER_FIRST_APPLY`). Keys should match [staging](https://supabase.com/dashboard/project/anqhihkvovuhfzsyqtxu/settings/api) and [prod](https://supabase.com/dashboard/project/wxupyzthtzeckgbzqmjy/settings/api) anon keys. |
| 2 | Vercel production branch | Vercel → Settings → Git. **Production branch** = `prod`. |
| 3 | Prod DB password | You have the prod DB password stored (or reset in Supabase) and used it in the prod connection string for Render. |
| 4 | Prod migrations | In **Prod → SQL Editor** run: `SELECT COUNT(*) FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'secret_refs';` → should return `1`. |
| 5 | Prod Vault | **Prod → Integrations → Vault → Secrets**: all 5 secrets listed (`github_token`, `openai_api_key`, `vercel_api_token`, `supabase_access_token`, `render_api_key`). |
| 6 | Render prod DATABASE_URL | Render → ai-factory-api-prod → Environment. **DATABASE_URL** is set (value is hidden). Prod service deploys and health check passes. |
