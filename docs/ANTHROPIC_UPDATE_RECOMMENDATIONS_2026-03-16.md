# Anthropic update recommendations — 2026-03-16

Generated by `scripts/anthropic-repo-supabase-render-report.mjs` (repo + Supabase + Render passed to Anthropic API).
Read this document for actionable updates.

**Clarification:** The "6–7 missing migrations" (graph_os 06/07/08, runs_repo_commit, policies_rules_json_guardrails, evolution_loop_v1, evolution_targets_seed) **are already registered** in `scripts/run-migrate.mjs`. The audit payload only sends the last 20 migration names, so the model sometimes infers a gap. Run `npm run verify:migrations` to confirm; see [docs/MIGRATIONS.md](MIGRATIONS.md) for intentional skips (core schema from schemas/001 and 002).

---

# AI Factory Full-Stack Review: Update Recommendations

## Summary

This AI Factory repo shows a mature full-stack architecture with Control Plane (Express), Console (Next.js), runners, and extensive Supabase migrations. The codebase demonstrates good practices with autonomous migration handling and self-healing capabilities. However, there are several areas needing attention: migration registration gaps, dependency security updates, configuration improvements, and documentation inconsistencies.

**Key Issues:**
- 6 migrations not registered in run-migrate.mjs
- Several outdated dependencies with potential security implications
- Missing health check configurations
- Inconsistent migration verification processes
- Documentation gaps around new features

## Repo & Dependencies

### Root Package Dependencies
- **Security Updates Needed:**
  - `@sentry/node: ^8.55.0` → Update to latest 8.x for security patches
  - `axios: ^1.8.0` → Should be `^1.7.9` to match control-plane (consistency issue)
  - `express: ^5.2.1` → Recently updated, good
  - `pg: ^8.13.1` → Current and secure

### Control Plane Dependencies
- **Outdated Sentry:** `@sentry/node: ^8.49.0` vs root's `^8.55.0` - should match
- **Good:** Most dependencies are current (Express 5.2.1, Supabase 2.98.0, Node 20+ requirement)

### Console Dependencies  
- **Generally Current:** Next.js 14.2.21, React 18, modern tooling
- **Large Bundle:** Many UI libraries - consider tree shaking optimization
- **Good Practices:** Proper TypeScript, Tailwind, modern React patterns

### Runners Package
- **Correct Approach:** Empty dependencies, inherits from root workspace
- **Documentation Accurate:** Clear instructions to run from repo root

## Supabase & Migrations

### Critical Migration Gap
**ISSUE:** 6 migrations exist in `supabase/migrations/` but are NOT registered in `scripts/run-migrate.mjs`:
1. `20250401000006_graph_os_reconciliation_action_policies.sql`
2. `20250401000007_graph_os_seed_action_policies.sql` 
3. `20250401000008_graph_os_approval_requests_v2.sql`
4. `20250402000000_runs_repo_commit_job_runs_next_retry.sql`
5. `20250402100000_policies_rules_json_guardrails.sql`
6. `20250404000000_evolution_loop_v1.sql`
7. `20250404000001_evolution_targets_seed.sql`

This means these migrations are **not applied** when Control Plane starts, potentially causing schema drift.

### Migration Verification Process
- `npm run verify:migrations` script exists - good practice
- Need to run this before every PR with migrations (mentioned in docs)
- The 80 vs 86 count discrepancy confirms the gap above

## Render & Deployment

### Service Configuration
**Good Practices:**
- All services use `dockerContext: .` (repo root) correctly  
- Health check paths properly configured (`/health` for API/Runner, `/health/readiness` for Gateway)
- Proper environment variable structure

**Missing Configuration:**
- No explicit resource limits specified
- Should add `DATABASE_POOL_MAX` defaults in render.yaml comments
- Consider adding explicit `HEALTHCHECK_TIMEOUT` for services

### Docker Strategy
- **Correct:** Single dockerfile per service with repo root context
- **Good:** Migrations run automatically on Control Plane startup
- **Missing:** No multi-stage builds for smaller images

## Security & Environment

### Environment Variables
**Well Structured:**
- Clear separation of required vs optional env vars
- Self-heal tokens properly documented
- Database pooling configurable

**Security Improvements Needed:**
- Add explicit health check timeouts to prevent hanging
- Consider adding `NODE_OPTIONS="--max-old-space-size=2048"` for memory management
- Add explicit rate limiting configurations

### Secret Management  
- Uses proper secret reference pattern with `secret_refs` table
- Vault integration documented
- Self-heal requires proper token setup (well documented)

## Documentation

### Strong Areas
- Comprehensive operations runbook
- Clear migration procedures  
- Self-heal documentation is thorough
- Architecture overview is detailed

### Gaps Identified
1. **Migration Verification:** docs/OPERATIONS_RUNBOOK.md mentions `npm run verify:migrations` but doesn't emphasize running it before EVERY PR with migrations
2. **New Features:** Evolution Loop V1 and Graph OS features documented but migration registration incomplete
3. **Troubleshooting:** Missing specific runbook for the migration registration gap scenario

## Recommended Actions

### 1. Fix Migration Registration (CRITICAL)
```bash
# Add these 6 missing migrations to scripts/run-migrate.mjs:
- graph_os_reconciliation_action_policies (20250401000006)
- graph_os_seed_action_policies (20250401000007)  
- graph_os_approval_requests_v2 (20250401000008)
- runs_repo_commit_job_runs_next_retry (20250402000000)
- policies_rules_json_guardrails (20250402100000)
- evolution_loop_v1 (20250404000000)
- evolution_targets_seed (20250404000001)
```

### 2. Dependency Updates
- Update `@sentry/node` to latest 8.x in both root and control-plane
- Align `axios` version between root (`^1.8.0`) and control-plane (`^1.7.9`)
- Run `npm audit` and address any HIGH/CRITICAL vulnerabilities

### 3. Add Migration Verification to CI
```json
// Add to root package.json scripts or GitHub Actions:
"pre-commit": "npm run verify:migrations && npm run lint"
```

### 4. Enhance Render Configuration  
Add to render.yaml service definitions:
```yaml
envVars:
  - key: DATABASE_POOL_MAX
    value: "5"
  - key: HEALTHCHECK_TIMEOUT  
    value: "30s"
```

### 5. Documentation Updates
- Update docs/OPERATIONS_RUNBOOK.md to emphasize `npm run verify:migrations` must run before EVERY migration PR
- Add troubleshooting section for "migration exists but not registered" scenario
- Document the Evolution Loop V1 and Graph OS features in relation to their migrations

### 6. Improve Migration Process
- Consider adding a pre-commit hook that runs `npm run verify:migrations`
- Add CI check that fails if migration files exist but aren't registered
- Document the specific order requirements for the migration array in scripts/run-migrate.mjs

### 7. Health Check Improvements
Add explicit timeout configurations to all Render services and document health check expectations in docs/RELIABILITY_OBSERVABILITY.md

### 8. Security Hardening
- Review and update any HTTP-only cookies to secure
- Ensure all external API calls use proper timeout configurations
- Add explicit CORS origin validation in production

**Priority Order:** Fix migration registration (#1) immediately, then dependency updates (#2), then configuration improvements (#3-4), finally documentation and process improvements (#5-8).

---

*End of report. Re-run the script to regenerate.*
